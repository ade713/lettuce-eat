services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: meal_nutrition
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d meal_nutrition"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
